extend basehome

block base
  .container
    .row
      .col
        h1 AutoMD - Diagnosis
        form(action='/submit-complaint' method='POST')        
            h4.spacing Symptoms
            hr
            select#symptomname.form-control(name='Symptoms')
              each so in symptoms
                option= so.symptom_name       
            hr 
            button(type='submit') Submit All for Diagnosis

